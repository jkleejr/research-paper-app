# Secrets — never commit API keys
PaperReader/Support/Secrets.xcconfig
*.xcconfig

# Xcode
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout

# macOS
.DS_Store

# Local secrets folder — never commit anything in here
.secrets/
